// Layer 3 — Workflow Engine. Durable execution; approval graphs;
// reversibility-gated execution; retry semantics; eval gates.
// Reversibility classes (idempotent / reversible / irreversible) are runtime
// invariants here, not guardrail prompts.
export {};
