{
  "name": "hypergaas-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "HyperGaaS monorepo — the agent SDK for B2B SaaS, its docs/specs, the landing site, and worked examples. Workspace root; not published.",
  "type": "module",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.17.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm --filter @hypergaas/core run build",
    "test": "pnpm --filter @hypergaas/core run test",
    "typecheck": "pnpm -r run typecheck",
    "clean": "pnpm -r run clean"
  }
}
