// Layer 2 — Memory OS. Typed, multi-layer, tenant-scoped by default.
// Layers: episodic / semantic / procedural / business-object / organization /
// workflow. Memory write-back is opinionated and automatic — bad memory
// hygiene is a framework failure, not a user error.
export {};
